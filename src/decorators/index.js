'use strict';

const { validateEventName } = require('./validateEventName');
const { validateCallback } = require('./validateCallback');

module.exports = {
  validateEventName,
  validateCallback,
};
